echo ok && echo passou

false && echo nao_aparece

false || echo executou

false && echo A || echo B

true && false || echo C

echo oi > file && echo depois

echo "a b c" | tr " " "\n" | wc -l

echo "abc" | grep z | wc -l

echo "abc" | grep a && echo OK

echo "abc" | grep z || echo fallback

echo "a b c" | tr " " "\n" | grep b && echo PASSOU

echo "abc" | grep a || echo ERRO

ls>fls>f1>f2>f3>f4>$USER -la | cat<Makefile>>$USER&&export OI=bruno&&echo $OI>>$USER

ls -la | cat<Makefile>>$USER&&export OI=bruno&&echo $OI>>$USER